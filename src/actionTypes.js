import React from "react"
import { useDispatch } from 'react-redux'

function dispatch(param) {
    
}

export const CART_ADD_PRODUCT_TOTAL_COUNT = () => {
    dispatch({
        type: "CART_ADD_PRODUCT_TOTAL_COUNT"
    })
}
